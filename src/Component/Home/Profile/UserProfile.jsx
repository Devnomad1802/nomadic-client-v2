import ProfileTabs from "../../SmallComponents/Tabs/ProfileTabs/ProfileTabs";
import { Container } from "@mui/material";

const UserProfile = () => {
  return (
    <Container>
      <ProfileTabs />
    </Container>
  );
};

export default UserProfile;
